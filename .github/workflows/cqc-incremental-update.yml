name: CQC Incremental Update

on:
  schedule:
    # Run daily at 11 PM UTC
    - cron: '0 23 * * *'
  workflow_dispatch:

permissions:
  contents: write
  packages: write

jobs:
  incremental-update:
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout main package repo
        uses: actions/checkout@v4
      
      - name: Setup R
        uses: r-lib/actions/setup-r@v2
        with:
          r-version: '4.3'
      
      - name: Install system dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y libcurl4-openssl-dev libssl-dev
      
      - name: Install R dependencies
        run: |
          R -e 'install.packages(c("httr", "jsonlite", "desc"), repos="https://cloud.r-project.org")'
      
      - name: Install cqcpack
        run: |
          R -e 'install.packages("remotes", repos="https://cloud.r-project.org"); remotes::install_local(".")'
      
      - name: Checkout data repo
        uses: actions/checkout@v4
        with:
          repository: Care-Policy-and-Evaluation-Centre/cqc-data-repo
          token: ${{ secrets.CQC_DATA_REPO_TOKEN }}
          path: cqc-data
          ref: main
      
      - name: Create data directories
        run: |
          mkdir -p cqc-data/incremental
      
      - name: Run incremental update
        env:
          R_CQC_API_KEY: ${{ secrets.R_CQC_API_KEY }}
          CQC_DATA_REPO_PATH: ${{ github.workspace }}/cqc-data
        run: |
          Rscript tests/helper-update.R

      - name: Rebuild package dataframes
        env:
          CQC_DATA_REPO_PATH: ${{ github.workspace }}/cqc-data
        run: |
          Rscript -e '
            library(cqcpack)
            update_location_dataset()
            update_provider_dataset()
            location_df <- build_location_df()
            provider_df <- build_provider_df()
            merged_df <- merge_provider_location()
            save(location_df, file="data/location_df.rda")
            save(provider_df, file="data/provider_df.rda")
            save(merged_df, file="data/merged_df.rda")
          '
      
      - name: Commit and push data repo
        working-directory: cqc-data
        run: |
          git config user.name "CQC Data Bot"
          git config user.email "noreply@github.com"
          git add -A
          git commit -m "CQC incremental update: $(date -u +%Y-%m-%d)" || echo "No changes"
          git push origin main
      
      - name: Commit and push package repo
        run: |
          git config user.name "CQC Data Bot"
          git config user.email "noreply@github.com"
          git add DESCRIPTION data/location_df.rda data/provider_df.rda data/merged_df.rda
          git commit -m "Package update with latest CQC data: $(date -u +%Y-%m-%d)" || echo "No changes"
          git push origin main